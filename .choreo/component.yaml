# .choreo/component.yaml
# Minimal source configuration so Choreo's "Source Configuration File Validation" passes.
name: legendary
version: "1.0.0"
type: service

build:
  dockerfile: Dockerfile
  context: .
  # If Choreo needs a build command, uncomment:
  # command:
  #   - bash
  #   - -lc
  #   - |
  #     pip install -r requirements.txt && \
  #     python -m py_compile legendary_empire_app.py && \
  #     python legendary_empire_app.py debug-imports

run:
  command:
    - python
    - legendary_empire_app.py
    - run-dashboard
  env:
    - name: SOLANA_RPC_URL
      value: https://api.mainnet-beta.solana.com
    # Required at runtime; set real values via Choreo environment variables:
    # - name: WALLET_PRIVATE_KEY
    #   value: "<base58_private_key>"
    # - name: PHANTOM_WALLET_ADDRESS
    #   value: "<public_address>"
    # Optional:
    # - name: TELEGRAM_BOT_TOKEN
    #   value: "<token>"
    # - name: TELEGRAM_CHAT_ID
    #   value: "<chat_id>"
